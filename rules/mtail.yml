groups:
- name: mtail Syslog NFS
  interval: 60s
  rules:
  - record: nfs_server:syslog_nfs_timeouts:rate1m
    expr: >
      sum by (nfs_server) (
        rate(syslog_nfs_timeouts_total[1m])
      )
