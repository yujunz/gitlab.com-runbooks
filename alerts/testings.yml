groups:
- name: testing.rules
  rules:
  - alert: HawaiianHugs20
    expr: node_load1{environment="prd",job="node",type="git"} > 1
    for: 1m
    labels:
      severity: critical
    annotations:
      description: "This one we should really worry about, really... it's git"
      node_load: '{{ $value }}'
      title: This is the git nodes doing things
  - alert: HawaiianHugs21
    expr: node_load1{environment="prd",job="node",type="api"} > 1
    for: 1m
    labels:
      severity: warn
    annotations:
      description: 'Nothing to worry about, at least not too much'
      node_load: '{{ $value }}'
      title: This is the API servers doing work
