## redis replication
ALERT redis_replication_is_down
  IF count(redis_connected_slaves)-sum(redis_connected_slaves) != 1
  FOR 1m
  LABELS {severity="critical", pager="pagerduty"}
  ANNOTATIONS {
    title="Redis replication is down 1 minute",
    description="Redis replication is down for more than 1 minute! Consider review redis replication status",
    runbook="troubleshooting/redis_replication.md"
  }
