## Target is down
ALERT target_is_down
  IF up{instance!~"ebs|license|lb1|dev|aptly|packages|version|backend1", job!~"blackbox"} == 0
  FOR 1m
  LABELS {severity="critical", pager="slack"}
  ANNOTATIONS {
    title="{{ $labels.job }}/{{ $labels.instance }} failed to be scraped for the last minute",
    description="Check prometheus targets on the https://prometheus.gitlab.com/targets page to undestand which targets are down",
    runbook="https://prometheus.gitlab.com/targets"
  }
